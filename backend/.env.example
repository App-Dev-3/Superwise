# =============================================================================
# SUPERWISE API ENVIRONMENT CONFIGURATION
# =============================================================================

# Application Settings
NODE_ENV="development"
PORT=8080
FRONTEND_HOST="http://localhost:3000"

# Email Domain Configuration
# You can add more allowed domains like this:
# ALLOWED_EMAIL_DOMAINS="fhstp.ac.at,example.com,gmail.com"
ALLOWED_EMAIL_DOMAINS="fhstp.ac.at"

# Database Configuration
DATABASE_URL="postgresql://<username>:<password>@localhost:5432/superwise?schema=public"

# Authentication Configuration
CLERK_JWKS_URI="https://vital-pelican-84.clerk.accounts.dev/.well-known/jwks.json"

# Business Logic Configuration
SUPERVISION_REQUEST_COOLDOWN_DAYS=2

# Cache Configuration (Redis)
REDIS_HOST="localhost"
REDIS_PORT=6379
USER_CACHE_TTL=1200000             # 20 minutes (in milliseconds)
TAG_SIMILARITY_CACHE_TTL=86400000  # 24 hours (in milliseconds)
CACHE_MAX_ITEMS=1000               # Maximum number of items in cache